describe('Street', () => {
  it('creates a street in Bezier City', () => {
    // Given a street descriptor

    street = {
      name: 'aleanallein',
      buildings:[
        {type: 'store'},
        {type: 'tower'},
        {type: 'mall'},
        {type: 'store'}
      ]
    }

    // When we render the street

    // Then we get 

    // 
  });
});